
# 🤝 FriendFinder AI — Умный поиск единомышленников

**FriendFinder AI** — это интеллектуальная система рекомендаций, разработанная для поиска людей со схожими интересами. В отличие от обычных фильтров, проект использует нейросеть (BERT) для понимания *смысла* текста, а не простого совпадения слов.

Приложение анализирует описание пользователя ("О себе") и находит наиболее подходящих собеседников из базы данных, используя алгоритмы кластеризации и косинусного сходства.

---

## 📂 Структура проекта

В проекте используются следующие ключевые файлы:

* **`app.py`** — Главный файл приложения (Frontend). Отвечает за веб-интерфейс на **Streamlit**, взаимодействие с пользователем и отображение результатов.
* **`bert_processor.py`** — "Мозг" проекта (Backend). Содержит класс `BERTProcessor`, который загружает модель, обрабатывает текст, создает эмбеддинги и выполняет поиск похожих анкет.
* **`model_analys.ipynb`** — Исследовательский ноутбук. В нем проводился разведочный анализ данных, подбор параметров кластеризации и визуализация тем.
* **`base_doc.xlsx`** — База данных пользователей (Excel). Содержит текстовые описания профилей.
* **`requirements.txt`** — Список всех необходимых библиотек для работы проекта.

---

## 🧠 Техническая часть и Анализ

### Используемые технологии

* **Язык:** Python 3.10+
* **NLP Модель:** `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` (BERT, обученный на 50+ языках).
* **Кластеризация:** K-Means (алгоритм k-средних).
* **Визуализация:** Plotly (интерактивные графики), WordCloud, Matplotlib.
* **Интерфейс:** Streamlit.

### Выводы исследования (из `model_analys.ipynb`)

В ходе анализа базы данных модель успешно разделила пользователей на **6 устойчивых кластеров интересов**:

1. **Духовность и Традиции:** Интерес к религии, православной культуре, семье.
2. **Искусство и Творчество:** Музыка, живопись, танцы, театр.
3. **Природа и Садоводство:** Ботаника, дача, животные, прогулки.
4. **Технологии и Гик-культура:** IT, игры, гаджеты, современная музыка.
5. **Романтика приключений:** Путешествия, море, астрономия, военная история.
6. **История и Этнография:** Изучение культур, народные традиции, история.

**Ключевое преимущество:** Модель понимает контекст. Например, она знает, что "писать код" и "программирование" — это одна тематика, даже если слова разные.

---

## 🚀 Руководство по запуску

Для запуска приложения на вашем локальном компьютере выполните следующие шаги:

### Шаг 1: Установка Python

Убедитесь, что у вас установлен Python (версии 3.8 или выше).
Проверить можно командой в терминале (командной строке):

```bash
python --version
```

### Шаг 2: Подготовка папки

Скачайте все файлы проекта в одну папку. Убедитесь, что файл `base_doc.xlsx` лежит рядом с `app.py`.

### Шаг 3: Установка зависимостей

Откройте терминал (командную строку) в папке с проектом и выполните команду:

```bash
pip install -r requirements.txt
```

*Эта команда скачает и установит все библиотеки (Streamlit, Torch, Transformers и др.), указанные в файле `requirements.txt`.*

### Шаг 4: Запуск приложения

После успешной установки библиотек запустите проект командой:

```bash
streamlit run app.py
```

Приложение автоматически откроется в вашем браузере по адресу `http://localhost:8501`.

---

## 💡 Как пользоваться приложением

1. **Заполните профиль:** Введите свое имя, возраст и подробный рассказ о себе (чем подробнее, тем точнее поиск).
2. **Нажмите "Найти":** Система обработает ваш текст и определит ваш психотип (кластер).
3. **Изучите результаты:**
   * Вы увидите, к какой группе интересов вас отнесла нейросеть.
   * Система покажет топ пользователей, наиболее похожих на вас.
4. **Дайте обратную связь:** Вы можете лайкнуть или пропустить предложенные анкеты. Приложение покажет итоговую статистику совместимости
